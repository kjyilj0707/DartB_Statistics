# 📊 통계학 3주차 정규과제

이번 다트비 8기 통계학 정규과제는 통계학의 기초를 부담 없이, 그러나 정확하게 익히기 위한 과제입니다.  
교재 전체를 빠르게 훑는 것이 아니라, 데이터 분석에 꼭 필요한 통계 개념만 골라 깊게 학습하는 것을 목표로 합니다.

이번 주는 아래의 **Statistics_3rd_TIL**에 해당하는 범위를 읽고 `학습 목표`에 맞게 공부하시면 됩니다.  
이번 주에는 표본에서 계산한 값이 매번 조금씩 달라질 수 있다는 점을 배웁니다.

> 💡 이번 주 목표는 **표본 하나에서 나온 숫자를 모집단의 정답처럼 말하면 왜 위험한지** 이해하는 것입니다.


## 📚 Statistics_3rd_TIL

### 3장. 통계분석의 기초
#### 3.4 확률
#### 3.5 이론적인 확률분포

### 4장. 추론통계~신뢰구간
#### 4.1 추론통계를 배우기 전에


## 📌 이번 주 교재 참고 범위

- **교재 범위:** p.69-97
- **핵심 범위:** 확률의 기초, 정규분포, 표본분포, 중심극한정리
- **읽을 때 집중할 질문:** "표본 하나에서 나온 숫자를 왜 그대로 모집단의 진실이라고 말할 수 없는가?"


## 🗓️ Study Schedule

| 주차 | 교재 범위 | 공부 범위 | 완료 여부 |
| ----- | ----- | ------------- | --------- |
| 1주차 | p.15-45 | 통계와 표본 | ✅ |
| 2주차 | p.48-68 | 데이터 유형과 기술통계 | ✅ |
| 3주차 | p.69-97 | 확률과 표본분포 | ✅ |
| 4주차 | p.98-115 | 신뢰구간과 표본크기 |  |
| 5주차 | p.117-145 | 가설검정 기초 |  |
| 6주차 | p.147-180 | 집단 비교와 비율 비교 |  |
| 7주차 | p.181-202 | 상관과 회귀 |  |

<br>

<!-- 여기까진 그대로 둬 주세요-->


# 1단계 개념 정리 ✍️

이번 주 교재 범위에서 본인이 중요하다고 생각한 개념 3개를 골라 정리해 주세요.  
고르기 어렵다면 아래 개념 중에서 선택해도 됩니다.

- 확률
- 확률분포
- 정규분포
- 표본분포
- 표본평균
- 중심극한정리

## 01.

```
개념 이름: 확률, 확률변수
개념 설명:
1. 확률
- 불확실한 사건이 일어날 가능성을 0과 1 사이의 숫자로 표현한 것.
- 현대 확률론은 표본공간(가능한 모든 결과의 집합) 안의 각 사건에 실수를 대응시켜 확률을 정의하는 방식

2. 확률변수
- 표본공간의 각 결과(사건)에 실수 값을 대응시키는 함수.
- 시행 결과 자체가 숫자가 아니더라도(예: 성공/실패), 확률변수를 이용해 숫자로 바꾸어 다룰 수 있게 해줌.
    1) 실현값
    - 확률변수가 실제 시행을 통해 구체적으로 가지는 값.
    2) 이산형 / 연속형
    - 실현값이 셀 수 있는 값 -> 이산형 확률변수, 특정 구간 내 모든 실수 ->  연속형 확률변수.

데이터 분석에서 중요한 이유:
- 데이터 분석에서 다루는 값(매출, 응답 시간, 설문 결과 등)은 표본마다 달라지는 불확실성을 가지므로 이를 확률변수로 모형화해야 통계적으로 분석할 수 있음.
- 확률 개념이 있어야 결과가 우연히 나온 차이인지 의미 있는 차이인지를 판단하는 근거를 만들 수 있음.
```


## 02.

```
개념 이름: 확률분포
개념 설명:
1. 확률분포
- 가로축에 확률변수의 값, 세로축에 그 값이 발생할 가능성을 나타낸 분포.
    1) 확률변수가 이산형인 경우
        - 세로축이 확률 그 자체를 나타냄 (확률질량함수).
    2) 확률변수가 연속형인 경우
        - 확률변수가 정확히 하나의 값을 가질 확률은 0이 됨.
        - 따라서 연속형 확률변수는 값에 일정한 구간을 두고 그 구간의 확률을 구함 → 확률밀도함수 이용.
        - 확률밀도함수의 세로축 값 자체는 확률이 아니라 상대적인 발생 가능성(밀도)을 나타내며 실제 확률은 특정 구간에서 이 곡선 아래의 면적(적분값)으로 구함.
    3) 추론통계와의 관계
        - 현실 세계의 모집단을 수학 세계의 확률분포로 가정하고 표본데이터는 그 확률분포에서 얻어진 실현값으로 간주하여 분석을 진행.
        - 이를 통해 모집단·표본데이터처럼 직접 다루기 어려운 대상을 확률분포나 실현값처럼 수학적으로 다룰 수 있는 대상으로 바꿈.
        - 흐름: 모집단[현실 세계] → 확률분포로 가정[수학 세계] → 표본 추출(실현값 관측) → 추정 → 모집단의 특성 파악

2. 기댓값
- 확률변수가 평균적으로 어떤 값을 가지는지를 나타낸 값.
- 확률변수의 기댓값은 E(X)로 표기.
    1) 이산형 - 각 실현값과 그 값이 발생할 확률을 곱하여 모두 더함.
       E(X) = Σᵢ xᵢ P(X = xᵢ)
    2) 연속형 - 실현값과 그에 대응하는 확률밀도를 곱한 후 확률변수가 정의된 전 범위에 대해 적분한 값.
       E(X) = ∫x f(x) dx

3. 분산, 표준편차
    1) 분산
    - 확률분포가 기댓값 주변에 어느 정도 퍼져 있는지를 나타내는 값. 각 실현값과 기댓값의 차이를 제곱한 값의 평균으로 계산.
        이산형)
        V(X) = Σᵢ (xᵢ - E(X))² P(X = xᵢ)
        연속형)
        V(X) = ∫(x - E(X))² f(x) dx
    
    2) 표준편차
    - 분산의 제곱근: σ = √V(X)
    - 분산은 값을 제곱해서 단위가 원래 데이터와 달라지므로 제곱근을 취해 원래 데이터와 같은 단위로 되돌린 값.

4. 왜도, 첨도
    1) 왜도
    - 분포가 좌우대칭에서 어느 정도, 어느 방향으로 벗어났는지를 나타내는 값. 양수면 오른쪽 꼬리가 길고 음수면 왼쪽 꼬리가 긺.

    2) 첨도
    - 분포가 정규분포에 비해 얼마나 뾰족하고 꼬리(극단값)가 두꺼운지를 나타내는 값. 첨도가 클수록 중심이 뾰족하고 꼬리가 두꺼워 극단값이 나올 가능성이 상대적으로 큼.

5. 조건부 확률
- 두 확률변수(또는 사건)를 함께 고려할 때 사용하는 개념.
- 확률변수 Y의 정보가 주어졌을 때 다른 확률변수 X가 특정 값을 가질 확률을 조건부확률이라 하며 P(X|Y)로 표기.
  P(X|Y) = P(X∩Y) / P(Y)
- X와 Y가 서로 독립인 경우에는 Y의 정보가 X의 확률에 영향을 주지 않으므로 P(X|Y) = P(X)

데이터 분석에서 중요한 이유:
- 확률분포를 알면 데이터가 어떤 값 주변에 모이고(기댓값) 얼마나 흩어져 있는지(분산·표준편차)를 하나의 수학적 모형으로 요약할 수 있음.
- 현실의 모집단을 확률분포로 가정하는 사고방식이 추론통계(표본으로 모집단을 추정하는 것)의 출발점이 됨.
- 조건부확률은 여러 변수 간의 관계(상관, 인과 추론 등)를 분석할 때 기본이 되는 개념임.
```


## 03.

```
개념 이름: 정규분포
개념 설명:
1. 정규분포 (가우스분포)
- 연속형 확률변수를 대상으로 정의됨.
- 확률밀도함수는 다음과 같이 나타냄.
  f(x) = 1/(σ√(2π)) · exp(-(x-μ)²/(2σ²))
- 확률분포가 평균(μ)과 표준편차(σ), 2개의 파라미터로 정해진다는 의미.

2. 표준정규분포
- 정규분포는 N(μ, σ²)로 표기하며(μ: 평균, σ²: 분산), 그 중 평균이 0이고 표준편차가 1인 것을 표준정규분포 N(0, 1)이라고 함.
- 특징
    1) 평균을 중심으로 한 종모양, 좌우대칭 분포
    2) 평균 근처에 값이 가장 많고, 평균에서 멀어질수록 적어짐
    3) 키나 몸무게 등 정규분포로 근사할 수 있는 현상이 많음

- 성질 (μ: 평균, σ: 표준편차)
    1) μ-σ 부터 μ+σ 까지의 범위에 값이 있을 확률은 약 68%
    2) μ-2σ 부터 μ+2σ 까지의 범위에 값이 있을 확률은 약 95%
    3) μ-3σ 부터 μ+3σ 까지의 범위에 값이 있을 확률은 약 99.7%

3. 표준화
- 확률변수 X(또는 데이터)를 평균과 표준편차를 이용해 변환하면 평균 0, 표준편차 1인 분포로 바꿀 수 있음.
  Z = (X - μ) / σ
- 평균과의 거리가 표준편차의 몇 배인지를 나타내므로 원래의 평균·표준편차와 상관없이 분포 안에서 어디에 위치하는지를 비교할 수 있음.

4. 다양한 확률분포
- 균등분포, 이항분포, 푸아송분포, 음이항분포, 지수분포, 가우스(정규)분포 등이 존재.

데이터 분석에서 중요한 이유:
- 중심극한정리에 의해 표본크기가 크면 표본평균의 분포가 정규분포에 가까워지므로, 정규분포의 성질(68-95-99.7 규칙 등)을 이용해 신뢰구간·가설검정 같은 추론통계 기법을 적용할 수 있음.
- 표준화(Z 변환)를 이용하면 단위나 척도가 다른 값들도 같은 기준(표준정규분포)에서 비교할 수 있기 때문에 이상치 판단이나 값 간 비교에 유용함.
```

## 04.
개념 이름: 모집단과 확률분포
개념 설명:
- 모집단분포 : 세로축이 '빈도(개수)'인 히스토그램
- 빈도/전체 요소 개수 = 비율
- 비율 = 확률
- 모집단분포 = 확률분포

- 모집단에서 무작위로 요소 하나 추출
= 각 요소가 자신의 확률만큼의 가능성으로 뽑힘. 
= 확률분포를 따라 값 하나가 발생하는 것과 동일한 조각
- 확률 분포를 따라 발생한 값 = 실현값(정의)
- => 표본 = 실현값.

데이터 분석에서 중요한 이유:
- 원래 문제 : "표본으로 모집단을 추정한다' -> 현실세계 개념을 수학적으로 접근하는 것. 
- 치환 후 : 실현 값으로 확률분포(의 모수)를 추정한다' -> 수학적개념으로 접근하기 때문에 공식 접근 등이 용이
=> 추론통계의 시작. 


# 2단계 확인 문제 ✅

## 문제 1. 표본평균의 변동

다트비 8기 전체 25명의 실제 평균 과제 소요 시간이 3시간이라고 가정합니다.  
운영진이 매번 5명씩 무작위로 뽑아 평균 과제 소요 시간을 계산한다고 할 때, 다음 질문에 답해 주세요.

1. 매번 뽑은 5명의 평균은 항상 3시간일까요? 
- X ( 매번 뽑는 5명이 다르기 때문에 평균도 달라짐)
2. 표본을 여러 번 뽑으면 표본평균들은 어떤 값 주변에 모일 가능성이 높나요?
- 모평균인 3시간 주변에 모인다. 표본 평균을 여러 번 구해서 평균을 내면 점점 모평균에 가까워지기 때문. 
- 또한 이표본 평균들의 퍼짐은 정규분포에 가까워진 모양을 뜰 것이다. 
3. 표본 크기를 5명에서 15명으로 늘리면 표본평균의 흔들림은 일반적으로 어떻게 달라질까요? 
- 적어진다. 표본 크기가 커질수록 각각의 표본들이 모집단 전체 정보를 더 많이 반영하게 되기 때문.  


## 문제 2. 정규분포 감각

어떤 시험 점수가 평균 70점, 표준편차 10점인 정규분포를 따른다고 가정합니다.

1. 80점은 평균보다 몇 표준편차 위에 있나요?
- +1(표준화 과정을 거쳐서 z점수를 구한다. 우선 원점수 차이 (평균과 80의 차이)인 10을 구하고 표준편차로 나눈다. )
2. 50점은 평균보다 몇 표준편차 아래에 있나요?
- -2 (1의 과정과 동일)
3. 50점과 80점 중 평균에서 더 멀리 떨어진 값은 무엇인가요?
- 50점


# 3단계 데이터 분석형 문제 🔍

## 문제 1. 표본 결과 비교하기

운영진이 8기 신입의 과제 소요 시간을 파악하기 위해 표본을 두 번 뽑았습니다.

| 조사 | 표본 크기 | 표본평균 |
| --- | ---: | ---: |
| 1차 조사 | 5명 | 2.4시간 |
| 2차 조사 | 15명 | 3.1시간 |

1. 두 표본평균이 다른 이유는 무엇일 수 있나요?
- 표본을 뽑을 때마다 우연히 어떤 사람들이 포함되느냐가 달라지기 때문에 표본평균값이 매번 달라진다. 또한 1차 조사 때와 2차조사 때 조사한 표본의 크기가 다른 것도 원인이 될 수도 있다. 
2. 둘 중 어느 조사 결과가 일반적으로 더 안정적이라고 볼 수 있나요?
- 표본이 많아질 수록 모집단을 반영하는 각각의 값들도 많아지기 떄문에 2차조사가 더욱 안정적이라고 볼 수 있다. 
3. 그래도 2차 조사 결과를 8기 전체의 정확한 평균이라고 단정할 수 없는 이유는 무엇인가요?
- 운영진은 15명이 아니라 25명이기 때문에 2차 조사 또한 전수조사가 아닌 표본조사일 뿐이고 따라서  2차 조사때의 표본과 표본평균값이 전체를 대표한다고 말하긴 어렵다. 8기 전체의 정확한 평균을 구하려면 25명의 각각의 시간을 모두 이용해서 모평균을 구해야 한다. 

## 문제 2. 해석 문장 작성하기

위 조사 결과를 바탕으로 운영진에게 전달할 문장을 2-3문장으로 작성해 주세요.
표본을 뽑을 때마다. 표본에 누가 포함되는지는 달라지기 때문에 표본평균값이 달라질 수 있습니다. 하지만 2차 조사때는 1차 조사보다 더 많은 표본을 통해 조사했기 때문에 상대적으로는 더 설득력있는 값으로 볼 수 있습니다. 하지만 이 또한 전체 모집단을 온전히 반영한다고는 보기 어렵습니다. 따라서 이 값에 대해서는 대략적으로 3.1시간 정도다"라고 추측하는 것이 올바릅니다. 또한 모집단의 크기가 크지 않기 때문에 전수조사를 통해 정확한 모평균값을 구하는 것을 추천드립니다. 


# 4단계 오류 찾기 🚨

아래 문장을 읽고, 통계적으로 부적절한 부분을 고쳐 써 주세요.

> "2차 조사에서 표본평균이 3.1시간이었으므로, 8기 전체의 평균 과제 소요 시간에 대해서 대략적으로 3.1시간이라고 추정할 수 있다. 하지만 이는 표본을 뽑을 때마다 달라질 수 있으므로 정확하지는 않다."

```
수정한 문장과 그 이유를 작성해 주세요.
- 원문은 표본평균인 3.1시간을 모평균처럼 보고 있다. 하지만 모평균과 표본평균은 다르고 표본평균은 그저 모집단의 일부인 표본을 이용해 모집단을 추정한 값이다. 따라서 3.1시간은 정확히 8기 전체의 평균과제 소요시간을 나타낸다"라고 말할 수 없다. 


```


### 🎉 수고하셨습니다.
