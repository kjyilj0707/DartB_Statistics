# 📊 통계학 2주차 정규과제

이번 다트비 8기 통계학 정규과제는 통계학의 기초를 부담 없이, 그러나 정확하게 익히기 위한 과제입니다.  
교재 전체를 빠르게 훑는 것이 아니라, 데이터 분석에 꼭 필요한 통계 개념만 골라 깊게 학습하는 것을 목표로 합니다.

이번 주는 아래의 **Statistics_2nd_TIL**에 해당하는 범위를 읽고 `학습 목표`에 맞게 공부하시면 됩니다.  
이번 주에는 데이터가 어떤 형태인지 확인하고, 그 데이터를 어떤 숫자와 그림으로 요약할 수 있는지 익혀봅니다.

> 💡 이번 주 목표는 **데이터의 유형과 분포를 보고 적절한 요약 방법을 고르는 것**입니다.


## 📚 Statistics_2nd_TIL

### 3장. 통계분석의 기초
#### 3.1 데이터 유형
#### 3.2 데이터 분포
#### 3.3 통계량


## 📌 이번 주 교재 참고 범위

- **교재 범위:** p.48-68
- **핵심 범위:** 데이터 유형, 변수의 종류, 데이터 분포, 히스토그램, 평균, 중앙값, 표준편차, 사분위수, 이상치
- **읽을 때 집중할 질문:** "이 데이터는 어떤 방식으로 요약하는 것이 가장 적절한가?"


## 🗓️ Study Schedule

| 주차 | 교재 범위 | 공부 범위 | 완료 여부 |
| ----- | ----- | ------------- | --------- |
| 1주차 | p.15-45 | 통계와 표본 | ✅ |
| 2주차 | p.48-68 | 데이터 유형과 기술통계 | ✅ |
| 3주차 | p.69-97 | 확률과 표본분포 |  |
| 4주차 | p.98-115 | 신뢰구간과 표본크기 |  |
| 5주차 | p.117-145 | 가설검정 기초 |  |
| 6주차 | p.147-180 | 집단 비교와 비율 비교 |  |
| 7주차 | p.181-202 | 상관과 회귀 |  |

<br>

<!-- 여기까진 그대로 둬 주세요-->


# 1단계 개념 정리 ✍️

이번 주 교재 범위에서 본인이 중요하다고 생각한 개념 3개를 골라 정리해 주세요.  
각 개념은 길게 요약하지 말고, 아래 형식에 맞춰 짧게 작성하면 됩니다.

고르기 어렵다면 아래 개념 중에서 선택해도 됩니다.

- 질적 변수
- 양적 변수
- 히스토그램
- 평균
- 중앙값
- 이상치

## 01.

```
개념 이름:변수
개념 설명:공통의 측정 방법으로 얻은 같은 성질의 값
[변수 유형]
1. 양적 변수
    1)이산형 변수 
    얻을 수 있는 값이 점점이 있는(띄엄띄엄 떨어진) 변수 
    예 - 자연수, 정수, 셀 수 있는 수... 

    2)연속형 변수
    간격 없이 이어지는 값으로 나타낼 수 있는 변수 
    예 - 키, 몸무게, 시간, 온도, 소득 등

2. 질적 변수 (범주형 변수)
값이 범주로 나타나며, 크기 비교가 의미 없는 변수
*그렇다면 범주가 아닌 문자열은?? => 범주의 크기 차원에서, 범주가 매우 큰 데이터일 수 있음. 
(대체적으로)범주의 크기는 크게 의미가 있지 않음 
예): 이름, 회원 ID, 주소 등..

데이터 분석에서 중요한 이유: 데이터 내의 변수들에 대한 이해를 바탕으로 변수들간의 관계를 파악하고, 데이터를 이해할 수 있음
- 데이터를 관찰하고 분석할 때, 변수 매우 중요
    변수가 1개-> 1변수 / 2개 -> 2변수 ...
    변수의 개수 -> "차원"과 관련됨 (변수 1개: 1차원, 2개:2차원 ...)(4개이상 : 고차원데이터)
    * 변수간의 관계를 밝힐 때 -> 한 대상으로부터 다른 변수들을 얻어야 변수들 간의 관계파악이 가능


```


## 02.

```
개념 이름:히스토그램(데이터 분포 표현)
개념 설명:
1.이산형 양적 변수의 히스토그램
    가로축 - 숫자 
    세로축 - 개수

2.연속형 양적 변수의 히스토그램
    *연속형 양적 변수는 소수점 이하 자리가 계속되기 때문에 엄밀하게 같은 값이 존재하는 것은 아님. 
    => 따라서 범위를 설정하고, 그 범위에 포함되는 숫자 개수를 세어 세로축에 둠. (이때 이 범위의 넓이 : 구간폭)

3.범주형 변수의 히스토그램
    가로축: 각 범주
    세로축: 개수

**주의할 것
<히스토그램의 목적과 한계>
-히스토그램은 데이터에 어떤 값이 얼마만큼의 빈도로 포함되어 있는지를 대략 파악하기 위함
-따라서 구체적인 결론을 내기 위한 목적이 아님
=> 데이터에 대한 계산을 수행하고, 수치적으로 다루는 통계분석 + 히스토그램 모두 필요

<구간 폭 설정의 문제>
-구간폭을 어떻게 설정하는지에 따라 히스토그램의 인상이 달라짐
-구간폭이 너무 좁다" -> 각 범위에 포함된 데이터들이 적어짐
-구간폭이 너무 넓다" -> 데이터가 어떻게 분포되어있는지의 정보가 빠짐
(구간 폭을 어떻게 설정해야 한다"와 같은 정해진 것은 따로 없음)


데이터 분석에서 중요한 이유:
데이터를 분석하기에 앞서, 데이터가 어떻게 분포되어있는지 봄으로써 대략적인 데이터의 경향을 파악하는 것이 필요하기 때문.
실제 EDA의 과정에서도 단변량, 이변량 분석 등을 하기 이전에, 데이터, 데이터 분포의 파악이 우선됨. 
대략적인 데이터의 구조, 분포를 파악함으로써 추후 구체적인 분석 방향을 정할 수 있음
```


## 03.

```
개념 이름:기술통계량(요약통계량)
개념 설명:
통계량 종류 (각 통계량마다 역할이 다름)
1.대푯값
-대략적인 분포 위치, 대표적인 값을 정량화하기 위해 사용함
    1)평균값(표본평균)
    *이상값의 영향 받음
    2)중앙값
    -크기순으로 값을 정렬했을 때, 한가운데 위치한 값
    3)최빈값
    -데이터 중 가장 자주 나타타는 값 (어떤 값이 정형적으로 나타나는지)
**이 대푯값들은 분포가 좌우대칭에 가깝다면 대체로 일치하지만, 좌우 비대칭 분포일 때에는 각각 다른 값이 되는 경향이 있음

2.분산과 표준편차
-데이터의 분포 형태를 표현하기 위한 차원
    1)분산(표본분산)
    표본의 각 값들과 표본평균이 어느정도 떨어져 있는지를 평가하는 지표
    (각 값과 평균값의 차이의 제곱의 합을 표본크기로 나눈것)

    2)표준편차
    분산에 제곱근을 취한것
    -> 원래 단위와 일치
**분포의 넓이가 넓어질수록 그에 따라 분산과 표준편차가 커짐

3.그림(그래프)
-데이터 분포를 시각화
    1)상자수염그림
    -중앙값기준으로 +-25%로 나뉨, 범위는 상자길이의 1.5배
    -범위 벗어난 값들은 표시
    2)오차막대
    -표준편차를 평균값에서 아래위로 늘인 오차막대
    3)바이올린플롯
    -히스토그램을 부드럽게 표현 -> 어디쯤 데이터가 존재하기 쉬운지 추정
    4)스웜플롯
    -겹치지 않는 각각의 점 -> 각 데이터가 어디에 있는지 자세하게 다룸

4.이상값
-명확한 정의는 없지만 평균값에서 표준편차의 2-3배 이상 벗어난 숫자를 이상값으로 파악
-> 시각화 통해 이상값 여부 확인


데이터 분석에서 중요한 이유: 히스토그램이 데이터가 어떻게 분포하고 파악하는 것이 목적이였다면 기술통계량은 데이터 그 자체의 성질을 기술하고 요약할 수 있음
데이터를 분석하기 위해서는 데이터의 분포를 파악하고, 그 분포 속의 데이터들의 성질들을 파악하는 것 모두 중요함. 
```


# 2단계 확인 문제 ✅

## 문제 1. 변수 유형과 요약 방식

다음 변수들이 `질적 변수`인지 `양적 변수`인지 구분하고, 적절한 요약 방식을 골라 주세요.  
요약 방식은 `빈도`, `비율`, `평균`, `중앙값`, `히스토그램` 중에서 고르면 됩니다.

| 변수 | 변수 유형 | 적절한 요약 방식 |
| --- | --- | --- |
| 통계학 과제 소요 시간 |양적변수 | 평균 |
| 과제 체감 난이도: 쉬움/보통/어려움 | 질적변수(범주형) | 빈도,비율|
| Python 사용 경험 여부 | 질적변수(범주형) | 비율 |
| 학술제 프로젝트 팀 인원 수 | 양적변수(이산형)| 평균,중앙값 |


## 문제 2. 대표값 계산

다음은 8명의 과제 소요 시간입니다.

```text
1, 2, 2, 3, 3, 3, 4, 10
```

1. 평균을 계산해 주세요. 3.5
2. 중앙값을 계산해 주세요. 3
3. 평균과 중앙값이 다르게 느껴지는 이유를 설명해 주세요.
10이라는 값이 나머지 값들에 비해 유독 큰 이상값임. 평균은 모든 값을 다 더한 후 나누는 과정을 통해 구하기 때문에 순서상 가운데에 위치한 값을 구하는 중앙값에 비해 더 큰 값이 나오게 되었다. 이렇게 이상값이 있을때에는 평균보다 중앙값이 더 적합한 대푯값이 될 것이다. 


# 3단계 데이터 분석형 문제 🔍

## 문제 1. 과제 부담 해석하기

다트비 8기 신입 10명의 2주차 과제 소요 시간이 아래와 같았습니다.

```text
1, 1, 2, 2, 2, 3, 3, 4, 5, 12
```

1. 평균과 중앙값을 계산해 주세요.3.5
2. 운영진에게 과제 부담을 보고한다면 평균과 중앙값 중 어떤 값을 더 강조하고 싶나요? 중앙값(2.5)를 강조한다. 다른 값들보다 유독 큰 12시간이라는 이상값때문에 평균이 3.5로 올라간 상태이기 때문에 값들의 순서를 통해 구한 중앙값이 더욱 적합하고, 2.5시간이 대부분의 신입의 과제 소요시간에 가깝다. 
3. 12시간이라는 값은 무조건 제거해야 할까요? 판단 기준을 적어 주세요.
우선 중앙값을 사용해 2.5시간을 대푯값으로 정하고, 12시간 또한 따로 언급해준다. 또한 이 12라는 값이 왜 나왔는지에 대한 분석을 이어가야 한다. 단순히 입력을 잘못한 것인지, 해당 신입이 정말 과제를 수행하는데 어려움을 느꼈는지 파악하고, 이후 해결해야 하는 방안을 모색해야한다. 

# 4단계 오류 찾기 🚨

아래 문장을 읽고, 통계적으로 부적절한 부분을 고쳐 써 주세요.

> "평균 과제 소요 시간이 3.5시간이므로, 대부분의 학회원은 과제를 3-4시간 안에 끝냈다고 볼 수 있다."

```
수정한 문장과 그 이유를 작성해 주세요.
평균 과제 소요시간은 3.5시간이지만 해당 평균에는 한 명의 이상치에 의해 끌어올려진 값이 있다. 실제로 대부분의 학회원들은 3시간 이하로 과제를 끝냈고, 중앙값은 2.5시간이다. 

평균이 3.5시간으로 대부분이 3-4시간안에 끝냈다라고 결론짓는것이 옳지 않다. 특히 해당 평균은 12시간이라는 이상값으로 인해 부풀려진 것도 있따. 따라서, 실제 10명의 학회원들의 소요시간을 보았을때 대부분이 3시간 이내로 과제를 끝냈고, 이상값의 큰 영향을 받지 않는 중앙값을 대푯값으로 사용해 2.5시간을 언급했다. 
```


### 🎉 수고하셨습니다.
